export enum AMOUNT {
  BEDRIDDEN = 1,
  SEDENTARY,
  LIGHT,
  MODERATE,
  HEAVY,
  ATHLETE,
}

export enum LEVEL {
  BEGINNER = 1,
  INTERMEDIATE,
  ADVANCED,
}

export enum GENDER {
  MALE = 1,
  FEMALE,
}

export enum LOGIN {
  GOOGLE = 1,
  FACEBOOK,
  LINE,
}
